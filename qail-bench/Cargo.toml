[package]
name = "qail-bench"
version = "0.8.11"
edition = "2021"

[[bench]]
name = "query_benchmark"
harness = false

[dependencies]
qail-core = { path = "../qail-core" }
criterion = { version = "0.5", features = ["html_reports"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[lints]
workspace = true
